namespace MentorMatch.Models;

public enum ProposalStatus
{
    Pending,
    UnderReview,
    Matched
}

public enum UserType
{
    Student,
    Supervisor,
    Admin
}

public enum ProjectType
{
    Individual,
    Group
}
